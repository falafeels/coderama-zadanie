import { css } from "@emotion/react";

export const iconsContainer = css({display: 'flex', alignItems: 'center', width: '100%', gap: '20px'})

export const ratingsIcons = css({display: 'flex', alignItems: 'center', gap: '8px'})